# Optional: base URL/path where the Gateway audio files are served.
# Default is "/audio" (i.e. files placed in gateway-app/public/audio/...).
# Point this at a CDN or storage bucket if you host the tapes elsewhere.
# NEXT_PUBLIC_AUDIO_BASE=https://your-bucket.example.com/gateway-audio
